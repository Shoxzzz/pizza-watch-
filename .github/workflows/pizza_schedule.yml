name: Pentagon Intel Suite (V32)

on:
  schedule:
    - cron: '0 * * * *' # 每小时整点
  workflow_dispatch:

concurrency:
  group: pentagon-intel
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  intel-job:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: 📥 拉取代码
      uses: actions/checkout@v4

    - name: 🐍 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10' 
        cache: 'pip'

    - name: 📦 安装依赖
      run: |
        pip install -r requirements.txt

    - name: 🍕 抓取披萨数据
      run: python -u monitor.py
      
    - name: 📰 抓取新闻数据
      run: python -u news.py

    - name: 💾 保存所有情报
      run: |
        git config --global user.name "Pentagon Bot V32"
        git config --global user.email "bot@pentagon.ops"
        
        # 同时提交两个 CSV
        git add pizza_data.csv pentagon_news.csv || true
        
        git commit -m "FULL INTEL UPDATE: $(date -u)" || echo "⚠️ 无新情报"
        
        git push
