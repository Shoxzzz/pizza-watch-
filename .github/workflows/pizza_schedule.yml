name: Pentagon Intel (Dual Agents)

on:
  schedule:
    - cron: '0 * * * *' # 每小时运行
  workflow_dispatch: # 手动运行按钮

concurrency:
  group: pentagon-intel
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  spy-job:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: 📥 拉取代码
      uses: actions/checkout@v4

    - name: 🐍 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10' 
        cache: 'pip'

    - name: 📦 安装依赖
      run: |
        pip install -r requirements.txt

    - name: 🍕 特工1：抓取披萨数据
      run: python -u monitor.py
      continue-on-error: true # 披萨抓失败了不要停，继续抓新闻

    - name: 📰 特工2：抓取新闻电讯
      run: python -u news.py
      continue-on-error: true

    - name: 💾 上传情报
      run: |
        git config --global user.name "Pentagon Bot"
        git config --global user.email "bot@pentagon.ops"
        
        # 强制添加两个文件
        git add pizza_data.csv pentagon_news.csv || true
        
        # 提交
        timestamp=$(date -u)
        git commit -m "INTEL UPDATE: $timestamp" || echo "⚠️ 无新数据"
        
        git push
